module.exports = {
  extends: ['@commitlint/config-conventional'],
  // rules: {
  //   'type-empty': [2, 'never'], // Type must not be empty
  //   'subject-empty': [2, 'never'], // Subject must not be empty
  //   'scope-empty': [1, 'always'], // Scope can be optional (set to warning)
  // },
};
