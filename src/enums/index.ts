export enum MasterEnum {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum textFieldVariantEnum {
  FILLED = 'filled',
  OUTLINED = 'outlined',
  STANDARD = 'standard',
}

export enum buttonVariantEnum {
  CONTAINED = 'contained',
  OUTLINED = 'outlined',
  TEXT = 'text',
}
